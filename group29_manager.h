#ifndef GROUP29_MANAGER_H
#define GROUP29_MANAGER_H

/*
 * Runs the Lab 6 virtual memory manager.
 *
 * Parameters:
 *   backing_store_path - path to BACKING_STORE.bin (or an equivalent binary)
 *
 * Returns:
 *   0 on success, non-zero on error.
 */
int run_lab6(const char *backing_store_path);

#endif
